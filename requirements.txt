fastapi>=0.111.0
uvicorn[standard]>=0.29.0
aiosqlite>=0.20.0
reportlab>=4.2.0
python-multipart>=0.0.31   # earlier versions have known vulnerabilities (pip-audit)
starlette>=1.3.1             # same
aiofiles>=23.2.1
pydantic>=2.7.0
bcrypt>=4.1.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
opencv-python-headless>=4.9.0
numpy>=1.26.0

# Postgres (Supabase) backend — optional; SQLite is used when DATABASE_URL
# is unset. See .env.example.
psycopg2-binary>=2.9.9
python-dotenv>=1.0.1

# Encryption at rest, report signing (Ed25519)
cryptography>=49.0.0   # 46.x has known vulnerabilities (pip-audit)

# Embedded PDF signatures
pyhanko>=0.30.0
