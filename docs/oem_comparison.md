# DVR/NVR OEM Comparative Analysis

This comparison is deliberately conservative. It distinguishes a confirmed on-disk signature from a brand label and does not turn unverified research into a recovery claim. The Dahua and Hikvision entries below are derived from this repository's format sheets.

All eight OEMs named in the SIH26150 problem statement have a registered plugin (see `backend/plugins/`). Public research exists for only three: **Dahua**, **Hikvision** and **Honeywell** (searched September 2026; see the sources in each format sheet). For **CP Plus** the only basis is Dahua compatibility. For **Uniview, TP-Link, Godrej and Matrix** no public format documentation or peer-reviewed analysis was found, so no vendor-specific parser exists; disks that no plugin recognises fall back to *generic standards-based stream carving* (MPEG-PS / H.264 Annex B — public standards only). That can recover video only if the recorder stores standard streams, and yields no timestamps or camera numbers. Guessed vendor offsets are refused by design (see "Evidence basis and limits").

| OEM | File system / container format publicly documented in this project | Known forensic recovery challenges | Current project support |
| --- | --- | --- | --- |
| **Dahua Technology** | **DHFS 4.1** is documented for DVR/NVR use; some NVR media may use XFS or mixed DHFS/XFS. The recoverable stream format documented here is **DHAV**, with `DHAV` frame starts and `dhav` frame footers. | The DHFS 4.1 index is documented in a public spec plus two independent open-source implementations, but on few disks and never on hardware here. Timestamps are local-clock (no timezone). Some header fields and the footer checksum remain unresolved. Interleaved multi-camera streams must be grouped by channel and time; proposed segmentation thresholds need validation on real media. | **Carving supported.** The Dahua plugin detects and carves DHAV frames; the DHAV layout was re-derived from FFmpeg's real `dhav.c` source and verified end-to-end on real ffmpeg-encoded video (see `format_sheets/dahua.md` §2). It also reads the DHFS 4.1 disk index (partition table, descriptors, cluster chains) from the Wullen spec, cross-checked against an independent extractor, to reassemble fragmented recordings with camera numbers and times; deleted/slack footage is carved separately. Not validated on a real Dahua disk. |
| **CP Plus** | No CP Plus-specific file system or container is verified. The Dahua format sheet records only an unverified compatibility hypothesis: a CP Plus disk containing **DHAV** frames can be handled as Dahua-formatted content. | A CP Plus nameplate does not establish a Dahua-compatible disk. Detect from the actual disk signature; non-DHAV media needs a separate validated parser. | **Conditional carving via Dahua compatibility.** `CPPlusPlugin` detects the CP Plus marker and, when DHAV frames are also present, routes carving through the Dahua engine — frames are tagged `brand="cpplus"` (not `"dahua"`) and segments stay `UNCERTAIN` with an explicit unverified note; there is no native CP Plus filesystem/container parsing. |
| **Hikvision** | Han, Jeong, Lee (2015, read in full): master sector at `0x200` with a documented field table, the **HIKBTREE** index of 48-byte data-block entries (channel, start/end UTC, block offset), 1 GB data blocks holding H.264 NAL units with `00 00 01 BA/BC` headers before each picture, and an `OFNI` IDR table. See `docs/format_sheets/hikvision.md`. | Field offsets come from one 2015 paper on one DVR model; the OFNI record layout and the bytes after `BA`/`BC` are unpublished, so per-frame timestamps are unavailable. A MDPI 2025 "HKVI" frame format was checked and rejected as uncorroborated. | **Indexed carving, decode-validated.** Detects the master sector, validates it with the paper's own arithmetic, reads HIKBTREE entries to give each 1 GB block a camera and a block-level time window, and carves all blocks (including ones the index no longer lists, e.g. after initialisation). Results start **UNCERTAIN**, become **PARTIAL** only if ffprobe decodes the export, never **COMPLETE**. Known-answer tests use the paper's printed values. **Not validated on a real Hikvision disk.** |
| **Honeywell Security** | Yoon & Hwang, [arXiv:2605.07430](https://arxiv.org/abs/2605.07430) (2026) analysed one Honeywell HN35080200 NVR by binary diffing: GPT disk, proprietary partition 1 with a video region at `0x80000000`, and video stored as **20-byte record headers + H.264 Annex B** (type, resolution, length, Unix-µs timestamp), each channel's data ending in a 20-zero-byte delimiter. Details: `docs/format_sheets/honeywell.md`. | Single device model in the paper; other models unvalidated. Formatting/expiry/overwrite leave the video region intact (paper §7), but the Video Channel List (which holds the camera number) is erased by a format, so the camera is unrecoverable by carving. | **Record carving from the published layout.** Detects a chain of valid records (0.9), carves records by structure (not offset), takes real timestamps from the headers, keeps separate on-disk streams separate, exports from the first IDR. Header decoding is checked against values printed in the paper; verified on real ffmpeg video with decoys. **Not validated on real Honeywell hardware**; index lists are not parsed. Results start UNCERTAIN and become PARTIAL only if ffprobe decodes them. |
| **TP-Link** | **No public documentation or research found** (searched 2026-09). | Unknown. | **Detection-only plugin + generic fallback.** A vendor-name string is reported as a weak hint (0.15). The disk is then processed by generic standards-based stream carving; nothing vendor-specific is parsed, so there are no timestamps or camera numbers, and video in a proprietary container will not be found. |
| **Godrej** | **No public documentation or research found** (searched 2026-09). | Unknown. | **Detection-only plugin + generic fallback.** A vendor-name string is reported as a weak hint (0.15). The disk is then processed by generic standards-based stream carving; nothing vendor-specific is parsed, so there are no timestamps or camera numbers, and video in a proprietary container will not be found. |
| **Uniview** | **No public documentation or research found** (searched 2026-09). | Unknown. | **Detection-only plugin + generic fallback.** A vendor-name string is reported as a weak hint (0.15). The disk is then processed by generic standards-based stream carving; nothing vendor-specific is parsed, so there are no timestamps or camera numbers, and video in a proprietary container will not be found. |
| **Matrix** | **No public documentation or research found** (searched 2026-09). | Unknown. | **Detection-only plugin + generic fallback.** A vendor-name string is reported as a weak hint (0.15). The disk is then processed by generic standards-based stream carving; nothing vendor-specific is parsed, so there are no timestamps or camera numbers, and video in a proprietary container will not be found. |

## Evidence basis and limits

- Dahua facts and limitations are taken from [`format_sheets/dahua.md`](format_sheets/dahua.md); items marked "TO VERIFY" there are not a basis for a forensic claim.
- Hikvision facts and limitations are taken from [`format_sheets/hikvision.md`](format_sheets/hikvision.md). In particular, the documented master-sector detector is not proof that a recovered stream is complete or admissible.
- Honeywell facts are taken from [`format_sheets/honeywell.md`](format_sheets/honeywell.md), which cites one paper about one device model.
- The TP-Link, Godrej, Uniview, and Matrix plugins are intentionally not populated with guessed formats, offsets, brand relationships, or recovery claims — their `detect()` markers are unverified vendor-name ASCII strings only, deliberately capped below the confidence needed to treat the brand as identified. Move an OEM to "supported" only after a public source or a representative real-disk sample supports the specific offsets/format implemented — not before.
- Nothing here has been validated on a real recorder disk; every recovered segment is labelled UNCERTAIN or PARTIAL, never COMPLETE, by carving alone.
